apiVersion: v1
kind: PersistentVolume
metadata:
  name: gv-postgres-pv-v2
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /var/lib/gv-postgres-v2
    type: DirectoryOrCreate
